---
"@mitumba/ui": minor
---

feat(forms): SearchFilterSheet — add optional showVaziFilter prop (default true) to hide the "VAZI Eligible Only" toggle, and make FilterState.vaziOnly optional. Backward compatible.

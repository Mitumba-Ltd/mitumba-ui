---
"@mitumba/ui": minor
---

feat(messaging): ChatThread `onTyping` prop — emits true on input, false after 2s idle or on send. Note: order attachments on received messages already work via `attachment: { type: 'order', data }` on MessageBubble (shipped in v0.19.0).
